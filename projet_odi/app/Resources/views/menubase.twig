<div class="container-fluid">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    {% if is_granted('ROLE_ADMIN')%}
        <a class="navbar-brand" href="/magasinier/produits/liste">ODI</a>
      </div>

      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><a href="/produits/liste/tout">Liste des produits</a></li>
          <li><a href="{{ path('ajoutProduit') }}">Ajouter un produit</a></li>
          <li><a href="{{ path('listeStocksProduit') }}">Gestion des stocks</a></li>
          <li><a href="{{ path('listePaniersMagasinier') }}">Gestion des paniers</a></li>
          <li><a href="{{ path('listeAlerte') }}">Alertes</a></li>
        </ul>
    {% elseif is_granted('ROLE_USER')%}
    <a class="navbar-brand" href="#">Client : {{ app.session.get('pseudo_personne') }}</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li><a href="/produits/liste/tout">Liste des produits</a></li>
      <li><a href="{{ path('panierClient') }}">Creation/Reprise d'un panier</a></li>
      <li><a href="{{ path('listePaniersClient') }}">Liste des paniers</a></li>
    </ul>
    {% else %}
        <a class="navbar-brand" href="/">ODI</a>
        </div>
          <ul class="nav navbar-nav">
            <li><a href="/produits/liste/tout">Produits</a></li>
          </ul>

    {% endif %}
    <ul class="nav navbar-nav navbar-right">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                 <li>
                     {% if app.session.has('id_personne') %}     <div class="alert alert-warning">           {{ app.session.get('id_personne') }}     </div> {% endif %}
                     <i class='fa fa-user-secret'></i>
                     {{ app.user.username }}
                     {{ app.session.get('pseudo_personne')}}
                </li>
                 <li><a href="{{ path('logout') }}">déconnexion</a></li>
            {% else %}
                 <li><a href=" {{ path('login') }}">connexion</a></li>
            {% endif %}
    </ul>
  </div>
</div>
